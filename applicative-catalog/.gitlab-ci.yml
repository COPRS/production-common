# This file is a template, and might need editing before it works on your project.
# Official docker image.
image: docker:latest

variables:
  MODULE_APP_CATALOG: s1pro-core-applicative-catalog

app-catalog-build-master:
  stage: module
  script:
    - cd applicative-catalog
    - docker build --network host --pull -t "$CI_REGISTRY/$CI_PROJECT_NAMESPACE/$MODULE_APP_CATALOG" .
    - docker push "$CI_REGISTRY/$CI_PROJECT_NAMESPACE/$MODULE_APP_CATALOG"
  only:
    - master

app-catalog-build:
  stage: module
  script:
    - cd applicative-catalog
    - docker build --network host --pull -t "$CI_REGISTRY/$CI_PROJECT_NAMESPACE/$MODULE_APP_CATALOG:$CI_COMMIT_REF_SLUG" .
    - docker push "$CI_REGISTRY/$CI_PROJECT_NAMESPACE/$MODULE_APP_CATALOG:$CI_COMMIT_REF_SLUG"
  except:
    - master
