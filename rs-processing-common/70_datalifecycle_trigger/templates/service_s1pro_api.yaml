kind: Service
apiVersion: v1
metadata:
  name: s1pro-data-lifecycle-svc
  labels:
    app: s1pro-data-lifecycle-svc
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
  - name: web
    port: 80
    targetPort: {{ .Values.service.port }}
