kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.processing.namespace }}
  generation: 1
  labels:
    app: {{ .Release.Name }}
  annotations:
    deployment.kubernetes.io/revision: '1'
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  replicas: {{ .Values.replicaCount.ingestion_edip_bedc_trigger }}
  template:
    metadata:
      name: {{ .Release.Name }}
      labels:
        app: {{ .Release.Name }}
        type: {{ .Values.processing.namespace }}
    spec:
      terminationGracePeriodSeconds: 30
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: {{ .Values.gateway.label.key }}
                operator: In
                values:
                - {{ .Values.gateway.label.value.edrs.bedc }}
      containers:
      - name: {{ .Release.Name }}
        image: {{ .Values.image.registry }}/{{ .Values.image.repository }}/{{ .Values.image.name }}:{{ .Values.image.tag }}
        imagePullPolicy: Always
        resources:
          requests:
            memory: {{ .Values.ingestion_edip_bedc_trigger.ram.request }}
            cpu: {{ .Values.ingestion_edip_bedc_trigger.cpu.request }}
          limits:
            memory: {{ .Values.ingestion_edip_bedc_trigger.ram.limit }}
            cpu: {{ .Values.ingestion_edip_bedc_trigger.cpu.limit }}
        volumeMounts:
        - name: service-logging-config-volume
          mountPath: /app/log
        - name: service-config-volume
          mountPath: /app/config
        - name: service-ssl-config-volume
          mountPath: /app/ssl
        env:
        - name: JAVA_XMS
          value: {{ .Values.ingestion_edip_bedc_trigger.javaOpts.xms }}
        - name: JAVA_XMX
          value: {{ .Values.ingestion_edip_bedc_trigger.javaOpts.xmx }}
        - name: edip.user
          valueFrom:
            secretKeyRef:
              name: edip-bedc
              key: USER
        - name: edip.pass
          valueFrom:
            secretKeyRef:
              name: edip-bedc
              key: PASS
        - name: mongodb.username
          valueFrom:
            secretKeyRef:
              name: mongodb
              key: USERNAME
        - name: mongodb.password
          valueFrom:
            secretKeyRef:
              name: mongodb
              key: PASSWORD
        envFrom:
        - configMapRef:
            name: s1pdgs-config
        - configMapRef:
            name: s1pdgs-logging-config
        - configMapRef:
            name: s1pdgs-env
        - configMapRef:
            name: s1pdgs-edip-bedc
        ports:
        - name: web
          protocol: TCP
          containerPort: {{ .Values.service.port }}
        securityContext:
          privileged: false
        livenessProbe:
          httpGet:
            path: /app/status
            port: {{ .Values.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
      tolerations:
      - key: {{ .Values.gateway.toleration.key }}
        operator: "Exists"
        effect: {{ .Values.gateway.toleration.effect }}
      imagePullSecrets:
      - name: {{ .Values.image.imagePullSecrets }}
      restartPolicy: Always
      dnsPolicy: ClusterFirst
      subdomain: {{ .Release.Name }}-svc
      securityContext: {}
      schedulerName: default-scheduler
      volumes:
        - name: service-logging-config-volume
          configMap:
            name: s1pdgs-logging-config
        - name: service-config-volume
          configMap:
            name: {{ .Release.Name }}-service-config
        - name: service-ssl-config-volume
          configMap:
            name: {{ .Release.Name }}-service-ssl-config
  serviceName: {{ .Release.Name }}-svc
  revisionHistoryLimit: 10
