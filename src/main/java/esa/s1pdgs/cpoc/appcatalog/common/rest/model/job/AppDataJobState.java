package esa.s1pdgs.cpoc.appcatalog.common.rest.model.job;

/**
 * Available states of job's applicative data
 * @author Viveris Technologies
 *
 */
public enum AppDataJobState {
    WAITING, DISPATCHING, GENERATING, TERMINATED;
}
