#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: s1pdgs-metadata-catalog
  profiles:
    active: dev
    
# Logging
logging:
  level:
    ROOT: ERROR
    org.springframework: WARN
    fr.viveris.s1pdgs: DEBUG
    kafka: WARN
    org.apache: WARN
  file: ./s1pdgs-metadata-catalog-test.log

#Object Storage
storage:
  user:
    id: MEF5PCBPTXHKOUTCKW3G
    secret: 4YiRYBN6HulfbA6uP7eZdhlm9Jb5JVYm18byMGJo
  endpoint: http://oss.eu-west-0.prod-cloud-ocb.orange-business.com/
  region: eu-west-0
  buckets:
    auxiliary-files: auxiliary-files
    session-files: session-files
    l0-acns: acns
    l0-slices: l0-slices

# KAFKA
kafka:
  bootstrap-servers: ${spring.embedded.kafka.brokers}
  group-id: metadata-catalog
  poll-timeout: 500
  topic:
    auxiliary-files: t-pdgs-auxiliary-files
    edrs-sessions: t-pdgs-edrs-sessions
    l0-slices: t-pdgs-l0-slices
    l0-acns: t-pdgs-l0-acns

# Elastic Search
elasticsearch:
  host: es-svc
  port: 25000
  index-type: metadata
    
# File
file:
  auxiliary-files:
    local-directory: ${ingestor_auxiliary-files_local-directory:/data/local-ingestor/auxiliary_files/} # Must contains the last /
  session-files:
    local-directory: ${ingestor_session-files_local-directory:/data/local-ingestor/edrs_sessions/} # Must contains the last /
  l0-slices:
    local-directory: ${ingestor_l0-slices_local-directory:/data/local-ingestor/l0_slices/} # Must contains the last /
  l0-acns:
    local-directory: ${ingestor_l0-acns_local-directory:/data/local-ingestor/l0_acns/} # Must contains the last /
    